export enum MusicTabs {
  Band = 'Band',
  Musicians = 'Musicians'
}

export enum UserData {
  UserBands = 'userBands',
  UserMusics = 'userMusics',
  UserAlbums = 'userAlbums',
}

export enum UserMusicalKeys {
  Bands = 'Bands',
  Musics = 'Musics',
  Albums = 'Albums',
}

export enum RelationTabs {
  Chat = 'Chat',
  Bands = 'Bandas',
}
